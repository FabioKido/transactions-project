/**
 * Camada 1
 * Entity
 * A entity representa uma regra de negócio e tem uma identificação única
 */

export default class Installment {

    constructor(readonly number: number, readonly amount: number) {}

}